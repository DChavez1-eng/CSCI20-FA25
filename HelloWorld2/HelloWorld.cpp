// cpp code
#include <cstdio> // include printf library
#include <string>

imt sum(int x,int y) { // define a fuction name: my_sum, output: int, inputs: int, int
    return x + y       // describes the output
}

/ anytime you use a function you must call it in the form <name>(<inputs>, <imputs)/

int main() {
    std::printf"("d\n", my_sum(1,1));
    return 0;
}